/*

The profile page lets the player view their own play stats, account settings, etc.

TODO: Everything. The page is pretty much blank right now
TODO: Backend to store userdata and accounts.

*/

import React from "react";

class Profile extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return <h1>Hello, this is the Profile page</h1>;
  }
}

export default Profile;
